<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <WebView
        android:id="@+id/webview"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </WebView>

    <TextView
        android:id="@+id/no_internet_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_margin="@dimen/no_internet_textMargin"
        android:padding="@dimen/no_internet_textPadding"
        android:text="@string/no_internet_connection"
        android:textAlignment="center"
        android:textStyle="bold"
        android:visibility="gone" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/share_Fab"
        android:layout_width="@dimen/fab_height"
        android:layout_height="@dimen/fab_width"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="@dimen/fab_margin"
        android:background="@color/darkPrimaryColorABNR"
        android:contentDescription="@string/fab_contentDescription"
        android:elevation="@dimen/fab_elevation"
        android:src="@drawable/ic_action_share" />

</RelativeLayout>